import Setup from "../components/Setup";
import Container from "../elements/Container";

const Settings = () => {
  return (
    <Container>
      <Setup />
    </Container>
  );
};

export default Settings;
